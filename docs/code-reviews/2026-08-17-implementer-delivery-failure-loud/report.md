# Code Review

- Reviewed: ak47-arch/workspace#10 (repo: ak47-arch/workspace, PR #10)
- Task: implementer-delivery-failure-loud · PRD: docs/prd-queue/2026-08-17-implementer-delivery-failure-loud.md · Review session: 06b04dee-2555-4b08-b458-7792eb9aeb7b
- Base: d556cafe0ca0e73d713a0d35a1b33b7370ae26cb → Head: bdac29e5483875fc1c5c41b8d5dea3f94d6db399

## Verdict

**REQUEST_CHANGES** — the code is functionally correct and every user story is verified green (all 4 stories, scope contained, regression green), but the **archived implementer report and decision records (D7) materially misdescribe the delivered diff**: they claim `push_and_pr` was switched to the `gh_call`/`IMPLEMENTER_GH_BIN` seam (decision 01, "chosen"), yet the diff shows `push_and_pr` is byte-for-byte unchanged w.r.t. `gh` — it still calls raw `gh`/`command -v gh`. The pr-fail test actually mocks `gh` on `PATH` (decision 01's explicitly-"rejected" option B), and decision 02 describes a `local pr_url` set-`u` fix that already existed in base. Since this is the safety-critical delivery path and the whole point of the task is truthful delivery, baking a false decision record into the knowledge base is a delivery-integrity defect. The correction is narrow (align the report + decision 01/02 with the actual diff, or implement the claimed seam); no code defect in the runtime path.

## Verification results

PRD acceptance command: `bash bin/test-implementer-driver.sh` (reads: existing 23 + new delivery-failure tests; "23" in the PRD is a nominal floor — the suite's actual count is 72).

| Command | Result | Evidence |
|---|---|---|
| `bash bin/test-implementer-driver.sh` | **72 passed, 0 failed** (exit 0) — `All tests passed.` | Includes the two new delivery-failure sections: **push-fail** (7 assertions: exit 1, `FAILED: delivery failed`, no `Pushed branch`, no `PR raised`, no `Done (exit 0)`, task reverted to `prd-ready`, transition-log `<prd-ready>`) and **pr-fail** (9 assertions: exit 1, `FAILED:` reason, honest `Pushed branch` present, no fake `PR raised`, no `Done`, revert to `prd-ready`, transition invoked, `gh pr create` attempted, branch present on the bare remote). |
| `bash -n bin/implementer-run.sh` | OK (syntax clean) | `set -euo pipefail` at line 46; guard at 1334–1336. |
| `bash -n bin/test-implementer-driver.sh` | OK (syntax clean) | |

Deferred (recorded, reason): real non-dry **successful** end-to-end delivery (real `git push` to `origin` + real `gh pr create` against ak47-arch) requires live GitHub credentials/network, which this sandbox deliberately lacks. The happy path is covered by the existing dry-run smoke and the non-dry `--revise` delivery tests (push to a real local bare remote); the PR-raising code is unchanged. Same for a real pushed-then-PR-failed run against `origin`.

## Story-by-story

- **[PASS] US1 — branch push fails → exit non-zero + clear reason, no "Done (exit 0)"**: `bin/implementer-run.sh` — `git push` is now guarded (`if ! git -C "$WORKTREE" push …; then echo "ERROR: git push … failed"; return 1; fi`, lines 663–668), so the function truthfully returns 1 instead of continuing past a failed push and printing `Pushed branch …`. The success path routes that to `fail_run`. Verified: delivery push-fail test — exit 1, `FAILED: delivery failed` present, no `Pushed branch`, no `Done (exit 0)`.
- **[PASS] US2 — push ok but `gh pr create` fails → exit non-zero + clear reason, branch left on remote**: the retry loop already returned 1 after 3 attempts (`ERROR: gh pr create failed after 3 attempts (branch remains pushed)`, return 1) — it previously had no effect only because the old success path swallowed it with `push_and_pr || true`. Now the guard routes it to `fail_run`; the branch is never rewound. Verified: delivery pr-fail test — exit 1, `FAILED: delivery failed`, `Pushed branch` printed (honest), no fake `PR raised`, branch present on the bare remote via `git for-each-ref`.
- **[PASS] US3 — delivery failure reverts task to `prd-ready` via existing `fail_run`**: the guard `if ! push_and_pr; then fail_run "delivery failed: branch push or PR creation"; fi` (1334–1336). `fail_run` (line 793) keeps existing semantics — `FAILED:` stderr, `stop_container`, `cleanup_run_dir --keep-logs`, archive full/partial report, `transition "prd-ready"`, `exit 1` (skipped in REVISION_MODE). Verified: both delivery tests assert the mock `transition-task.sh` received `--to prd-ready` (log `<prd-ready>`) and `docs/tasks/deliv.md` shows `**Status**: prd-ready`.
- **[PASS] US4 — successful delivery unchanged**: no change to the happy path; the host commit + push + `gh pr create --label factory:needs-review` remain. Verified: all pre-existing tests stay green (the suite went from the prior baseline to 72 passed, 0 failed) including the dry-run success smoke and the non-dry `--revise` delivery (still no new PR, D4 respected).

## Deterministic checks

- **[PASS] D1 — PR metadata sane**: head commit `bdac29e implementer(implementer-delivery-failure-loud): run 8370f85b-…[factory]` — well-formed factory PR on branch `factory/implementer-delivery-failure-loud`; base `d556caf` and head `bdac29e` both resolve; diff is non-empty and scoped to 2 files.
- **[PASS] D2 — Worktree clean / read-only**: `git status` → `HEAD detached at bdac29e`, `nothing to commit, working tree clean`; re-verified clean at finish — no reviewer mutations.
- **[PASS] D3 — Scope containment**: changed files are exactly `bin/implementer-run.sh` and `bin/test-implementer-driver.sh` — both in the PRD file map. Untouched as required: `bin/review-run.sh`, `bin/factory-run.sh`, `bin/transition-task.sh`, `config/*.json`, `.github/workflows/factory.yml`. No stray deps/refactors.
- **[PASS] D4 — Scope ⊆ PRD file-map**: both files the PRD claims are present in the diff; nothing promised is missing.
- **[PASS] D5 — Story → diff coverage**: US1→push guard (implementer-run.sh) + push-fail test; US2→main guard + pr-fail test; US3→fail_run routing + revert assertions; US4→happy path unchanged + green regression. One minor out-of-narrative capability: the `MANIFEST_SRC` fallback in `setup_fixture` (test file, lines 70–85) — a portability fix so the pre-existing suite runs in a standalone software-factory checkout (no `workspace-portability/` present here). It is inside the mapped file and is load-bearing for the suite to be green in this sandbox, so not blocking; flagged advisory.
- **[PASS] D6 — No secrets / stray deps**: diff scan for `api_key|secret|token|PRIVATE KEY` clean; the only credential is the test placeholder `OPENROUTER_API_KEY=sk-test` (fake). No `.env`, no `package/gem/requirements/go.mod` files changed.
- **[FAIL] D7 — Implementer report matches actual diff**: the archived report (`docs/implementations/2026-08-17-implementer-delivery-failure-loud/report.md`) and `decisions/01-delivery-failure-loud-gh-seam.md` claim `push_and_pr` "now calls `gh_call pr create` and checks `command -v "${IMPLEMENTER_GH_BIN:-gh}"` instead of the raw host `gh`" and "`gh label create` → `gh_call label create … || true`". **This is not in the diff** — `push_and_pr` (lines 684, 692, 701) still uses raw `command -v gh`, `gh label create`, `gh pr create`, identical to base. The pr-fail test instead mocks `gh` on `PATH` (`PATH="$DELIV:$PATH"` + `write_mock_gh`), which is precisely **option B that decision 01 explicitly rejects**. Additionally `decisions/02-…-local-nounset.md` describes adding `local pr_url=""`, but base already had `local attempt=0 pr_url=""` (line 691) — a non-change. The report also states "71 passed" vs the verified 72. The tests are real and green, and the PRD's essential testing requirement (a failing-push and a failing-`gh pr create` mock with the stated assertions) is met via PATH mocking — but the documented mechanism does not match the shipped code.

## Judgment checks

- **[PASS] J1 — Story intent**: each story's observable behavior genuinely holds from the diff: a failed push surfaces a non-zero + `FAILED:`, a failed PR leaves the branch on the remote and reverts the task, the happy path is unaltered. The subtle `set -e` + explicit-return / `if !` guard interaction is correctly reasoned and demonstrated by the passing tests.
- **[FAIL] J2 — PRD-decision conformance**: the PRD's Testing decisions name the `IMPLEMENTER_GH_BIN` seam for the failing-`gh pr create` mock; the implementation uses a PATH-injected `gh` stand-in instead, and the archived **accepted** decision 01 claims the PRD's seam was implemented when it wasn't (it even documents rejecting the approach actually used). Functionally equivalent and tests pass, but the implementation diverges from the stated seam AND from its own archived decision without a truthful rationale on record.
- **[PASS] J3 — Edge / error paths**: push failure → explicit ERROR + return 1; commit failure → return 2 (pre-existing); `gh` absent → the raw `command -v gh` guard returns 1 (no crash); `gh pr create` retries 3× with backoff then returns 1 (branch stays pushed); `fail_run` handles no-report (partial report) and full-report cases; REVISION_MODE still skips the lifecycle transition (D3 preserved). Guard placement under `set -euo pipefail` is correct (function called from `if !`, so explicit returns are the reliable fail path).
- **[PASS] J4 — Ponytail over-engineering pass (ultra)**: see Advisory findings. The delivery-failure tests legitimately need a full non-dry `main` run with mocked podman/gh/transition, so substantial fixture scaffolding is justified; the only clear duplication is the new `setup_delivery_fixture` vs the existing `setup_fixture` (shrink), plus a marginally redundant mock-transition status rewrite (yagni). The runtime fix itself is the minimal, correct change. (`ponytail-review`/`ponytail-debt` tools were not present as standalone skills in this container; the pass was applied manually in the documented `delete/stdlib/native/yagni/shrink` format.)
- **[PASS] J5 — UAT gaps**: enumerated below; chiefly a real end-to-end successful delivery and a real pushed-then-PR-failed run (both need live GitHub), plus the report/decision correction from D7/J2.

## Findings

### Blocking (→ REQUEST_CHANGES)
- `docs/implementations/…/decisions/01-delivery-failure-loud-gh-seam.md` + `…/report.md` (Story 2 note): **D7 / J2 delivery-integrity** — the archived record asserts `push_and_pr` was switched to the `gh_call` / `IMPLEMENTER_GH_BIN` seam and that `gh label create` became `gh_call label create`, but the diff contains **no such change** (raw `gh`/`command -v gh` unchanged from base at implementer-run.sh:684,692,701). The shipped pr-fail test mocks `gh` on `PATH` — the option the accepted decision 01 explicitly rejects. A future session reading decision 01 would be actively misled about the delivery path's mechanics. Fix: align the archived report + decision 01 with the actual diff — either implement the claimed `gh_call` seam, or truthfully document that the tests mock `gh` on `PATH` and why that was chosen over the PRD's stated seam.
- `…/decisions/02-delivery-failure-loud-local-nounset.md`: describes adding `local pr_url=""`, but base already shipped `local attempt=0 pr_url=""` (d556caf:691) — no such change is in the diff. Either implement the claimed initialization inline (if the run really hit `set -u`), or correct the decision record to not claim a fix that isn't present. (Not itself a runtime defect — the shipped code is sound under `set -u`.)

### Advisory (consider / over-engineering)
- `bin/test-implementer-driver.sh` (new `setup_delivery_fixture`): `shrink` — largely re-implements `setup_fixture`'s scaffolding (mktemp, git init + base commit, config copy, manifest, `.factory/runs`) instead of reusing it; the true deltas are the output `mode` (push-fail vs pr-fail origin) and the non-dry full-`main` run. Parameterizing `setup_fixture` with a mode/origin could remove the duplication. Never alone blocking.
- `bin/test-implementer-driver.sh` (`write_mock_transition`): `yagni` — the `sed -i "s/^\*\*Status\*\*…"` rewrite of `docs/tasks/deliv.md` is redundant with the `MOCK_TRANSITION_LOG` `<prd-ready>` assertion; the log alone already proves the transition fired, and the driver's `exit 1` + partial-report behavior covers the lifecycle side-effect. Keeping it is a (stronger) end-to-end check, so optional. Never alone blocking.
- `bin/test-implementer-driver.sh` (`setup_fixture` MANIFEST_SRC fallback): out-of-narrative-scope capability for this PRD (not a defense of any delivery story) but inside the mapped file and needed for the suite to be green in a standalone checkout; keep, but note it in the report's rationale.

## Ponytail debt (harvested from changed files)

No ponytail: debt. (`grep -rnE '(#|//) ?ponytail:'` over `bin/implementer-run.sh` and `bin/test-implementer-driver.sh` → no markers.)

## UAT hand-off list

1. **Correct the archived report + decisions (blocking, from D7/J2)**: make `report.md` + `decisions/01-delivery-failure-loud-gh-seam.md` + `02-…-local-nounset.md` (and their `docs/knowledge/sessions/…` copies) reflect the actual diff — the pr-fail test mocks `gh` on `PATH`, `push_and_pr` still uses raw `gh`, and the `local pr_url=""` init already existed in base. Either implement the claimed `gh_call` seam or document the PATH-mock choice truthfully.
2. **Real end-to-end successful delivery**: confirm a real driver run still delivers normally (host-authored commit, push, PR with `factory:needs-review`, exit 0) against the live ak47-arch remote — needs real GitHub creds, deferred here.
3. **Real pushed-then-PR-failed run**: confirm a real run leaves the branch on `origin`, prints `FAILED: delivery failed: branch push or PR creation`, and reverts the task to `prd-ready` — tested only against a local bare remote mock here.
4. **`workspace-portability` manifest**: the pre-existing `resolve_repo` → `public-release` assertion depends on `workspace-portability/workspace_restore_manifest.json`; here the new fallback keeps the suite green standalone, but confirm the real host (where the sibling repo lives inside the repo root) behaves unchanged.
5. **Advisory (non-blocking)**: consider folding `setup_delivery_fixture` into `setup_fixture` (shrink) and dropping the redundant transition-status rewrite (yagni).
